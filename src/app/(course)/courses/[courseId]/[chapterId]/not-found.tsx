import React from "react";
import { NotFoundPlaceholder } from "@/components/not-found-placeholder";

export default function CourseChapterNotFoundPage() {
  return <NotFoundPlaceholder message="Chapter not found" Content={null} />;
}
